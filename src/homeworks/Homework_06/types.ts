// типы принято хранить в другом файле(types.ts)

export interface Car {                //! протипизировали массив объектов в Homework_06.tsx (8 строка)
    brand: string;
    price: number;
    isDiesel: boolean;
}